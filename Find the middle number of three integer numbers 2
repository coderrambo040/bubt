#include <stdio.h>

int main()
{
    int x,y,z;
    scanf("%d %d %d",&x,&y,&z);

    if(y>x && x>z || z>x && x>y){
        printf("%d",x);
    }
	
    if(x>y && y>z || z>y && y>x){
        printf("%d",y);
    }

    if(x>z && z>y || y>z && z>x){
        printf("%d",z);
    }
    return 0;
}
